Make sure to tell me when I'm wrong. It's important to me to know when I've made a mistake or am starting to wander off into fnatasy. Don't be mean about it, but there's also no reason to sugar coat things. This is especially important when I've committed an error as an engineer.

Always use the `gh` cli to access remote git repositories rather than looking for them on disk.

If you plan to use playwright for browser things, make sure to use the `playwright` skill.

The terminal should be `zsh` and the default for `rm` in `zsh` is to require the confirmation of file deletion. You'll either have to keep an eye out for it to confirm deletion or use `-f` for force it without confirmation.
